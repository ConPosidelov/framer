//описывает timeLine панель
// timePoint - текущая точка выбранная на панели

const initial = {
    timePoint: 0
};



const timeLineControl = (state = initial, action) => {

    switch (action.type) {
        case 'VEW_LAYER':
        
        default:
            return state
    } 
};

export default timeLineControl;