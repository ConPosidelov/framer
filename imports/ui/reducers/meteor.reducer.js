//
const initial = {
    loading: false
    
};



const meteor = (state = initial, action) => {

    switch (action.type) {
        case 'LOADING_METEOR':
        
        default:
            return state
    } 
};

export default meteor;